<section class="resume-section">
  <div class="resume-section-content">
    <h1 class="mb-0">
      Cadastre-se
    </h1>
    <div class="subheading mb-5">
      Digite seu e-mail para
      <a href="#">cadastrar-se</a> ou gerar um novo <a href="#">Token</a>

      <%= simple_form_for :user, url: registration_path, method: :post do |f| %>
        <%= f.input :email %><br />
        <%= f.button :submit, 'Enviar', class: 'btn btn-secondary' %>
      <% end %>
    </div>
  </div>
</section>